# requirements.txt — NerfVision
# Proyecto Final, Visión por Computador
# Universidad Nacional de Colombia — Sede de La Paz
# Python 3.10+

ultralytics==8.3.0        # YOLO11 + ByteTrack (tracking integrado)
opencv-python-headless==4.10.0.84  # lectura de video/cámara y anotación de frames
numpy==1.26.4             # numpy<2.1 evita el conflicto con numba en Colab
streamlit==1.40.0         # interfaz de usuario
matplotlib==3.9.2         # gráficas de métricas y trayectorias
lap==0.5.13               # algoritmo de asignación para tracking


# IMPORTANTE: instalar SIEMPRE con el archivo de overrides:
#   uv pip install -r requirements.txt --overrides overrides.txt
# ultralytics declara opencv-python (con GUI) como dependencia; el override
# lo excluye para que solo quede el headless. NO desinstalar opencv-python
# a mano: ambos paquetes comparten el directorio cv2/ y desinstalar uno
# borra los archivos del otro (deja el entorno sin cv2).

# Nota: torch / torchvision NO se fijan aquí a propósito.
# ultralytics los instala como dependencia, y en Colab ya vienen
# con la build correcta para la GPU. Fijarlos a la fuerza puede
# romper el soporte CUDA.

# Nota: label-studio se instala en un entorno virtual APARTE
# (ver README). Es pesado y conviene no mezclarlo con la app.
